# npm
node_modules

# App specific
build

# The translations are build from
# locales/<LOCALE> files.
# They need to be ignored by git,
# but not by platforms like Github Pages.
# Files prefixed with `_` are ignored by default.
translations_*.json
package-lock.json

# template
src/static/css/report_template/*
src/static/html/report_template/*
